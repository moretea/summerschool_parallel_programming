use StdIO: all;
use Array: all;

int main() {
  printf("\nreshape( [5], [1,2,3,4,5]))\n");
  print(reshape( [5], [1,2,3,4,5]));

  printf("\nreshape( [3,2], [1,2,3,4,5,6])\n");
  print(reshape( [3,2], [1,2,3,4,5,6]));

  printf("\nreshape( [3,2,1], [1,2,3,4,5,6])\n");
  print(reshape( [3,2,1], [1,2,3,4,5,6]));

  printf("Wrong dimensions\n");
  /* print(reshape( [3,2,2], [1,2,3,4,5,6])); */
  /* Gives us a compiler error */

  return(0);
}
